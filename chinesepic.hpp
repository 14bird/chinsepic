#ifndef __CHINESEPIC_HPP
    #define __CHINESEPIC_HPP 1
    #include<string>
    #ifdef __unix__
        void makepic(std::string a,std::string name,std::string bc,std::string fc);
    #else
    #endif
#endif
